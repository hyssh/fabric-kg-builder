"""Fabric Ontology compiler and ID management.

Compiles ontology/model.yaml + ontology/ids.lock.json into
Fabric Ontology definition parts (EntityTypes/, RelationshipTypes/,
definition.json, .platform). IDs are deterministic and stable
across environments.
"""
