"""Post-deployment validation against local artifacts and rules.

Validates deployed Fabric Lakehouse tables, Fabric Ontology, and
Azure AI Search indexes against local build artifacts. Reports
pass/fail per rule with severity (fail vs. warn).
"""
