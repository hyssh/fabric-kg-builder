# Session Log: Specification & Implementation Planning
**Date:** 2026-06-24T20:49:56Z  
**Session Type:** Spec Authoring + Planning + Consistency Review  
**Duration:** Full spec/plan cycle

---

## Summary

This session completed the full specification authoring and planning cycle for the fabric-kg-builder project:

### Specifications Authored & Reconciled

- **SPEC-001 (Architecture & CLI, v4):** Locked. Click CLI, 11-stage pipeline (domain-intake → inspect-source → enrich → compile-data → compile-ontology → compile-search → package → deploy-lakehouse → deploy-ontology → deploy-search → validate). Foundry SDK (replaces raw OpenAI), Microsoft Foundry project config, Document Intelligence required, domain-intake security constraint.

- **SPEC-002 (Canonical Data Model, v1.2):** Locked. Deterministic SHA-256 row IDs, pinned canonical_key normalization, native Parquet arrays, placeholder layout, validation severity split (22 fail / 8 warn). Graph-to-Search bridge with denormalized search_aliases (entities) and ntity_search_keys (chunks) columns. Structured tables Lakehouse-only (never indexed to AI Search).

- **SPEC-003 (Ontology & Deployment, v2):** Locked. blob_url as node property (not edge), explicit inverse relationships with inversePolicy, environment config injected at compile time, prefix-based ID ranges. Graph-to-Search bridge (SPEC-003 §12) canonical binding to AI Search index. Created ontology/environments/{dev,test,prod}.json with no secrets.

- **SPEC-004 (LLM Enrichment, v2):** Locked. LLM output intermediate JSON only, Blob URLs runner-injected, confidence thresholds (0.70 / 0.50), Foundry SDK (AIProjectClient), model defaults locked (chat GPT-5.5-mini target / gpt-4.1 interim dev, embedding text-embedding-3-large@1536, vision = chat multimodal).

- **SPEC-005 (Validation & Test Plan, v3):** Locked. Stable VAL rule numbering, severity split (21 fail / 1 warn), no live API calls (all mocked), fixtures in 	ests/fixtures/, 90% coverage gate (src/ only), smoke tests post-deploy (not merge gate). Graph-to-Search contract fixture canonical form: search.in(entity_ids, ...).

### Infrastructure & Research

- **INFRA-001 (Azure Resources):** Dev/test resources verified in Example-Subscription / example-rg. Discovered: example-aiservices (Foundry), examplestorageacct (Blob), example-search (AI Search), example-docintell (Document Intelligence), example-vision. Gap identified: GPT-5.5-mini not yet deployed (interim: use gpt-4.1 for dev).

- **RESEARCH-001 (KG+AI Search Grounding):** Findings folded into SPEC-002 §11 (Graph-to-Search Clue Chaining) and SPEC-003 §12 (Graph-to-Search Bridge — Ontology Support). No standalone research doc needed; findings are now spec-canonical.

### Design & Planning

- **DESIGN-001 (Retrieval Agent Orchestration):** Proposed (M5, post-MVP). Foundry Agent Service + deterministic etrieve_grounding function tool. LLM never touches OData. MCP as graduation path, not starting point.

- **PLAN-001 (Implementation Plan):** 59 tasks across 6 milestones (M0–M5). Sprint 1 = 37 tasks (Skeleton, Data Model, Enrichment, Ontology). Sprint 2 = 22 tasks (Doc Extraction, Visual Assets, Enrichment E2E, AI Search). Critical path traced. All 13 PRD §23 acceptance criteria locked as MVPs.

### Decision Records

- **decisions.md:** Consolidated from 21 inbox files. Organized by topic: Architecture/CLI, Data Model, Ontology, LLM/Foundry, Infra/Azure, Canonical Naming, Plan, Retrieval Agent. 25.1KB.

### Consistency Review

- **5 CRITICAL findings resolved:**
  1. compile-search-index → compile-search (canonical name)
  2. ingest → inspect-source (canonical stage name)
  3. SPEC-003 §12 bridge references non-existent SPEC-002 columns (phantom columns removed, canonical binding established)
  4. SPEC-005 graph-to-search fixture uses ntity_ids/any(...) instead of search.in(...) (rewritten to canonical form)
  5. PRD superseded by specs for SDK, command names, model choices (banner added)

- **4 MEDIUM findings resolved:**
  1. Environment variable names inconsistent (AZURE_FOUNDRY_* → AZURE_AI_FOUNDRY_*)
  2. config key names inconsistent across specs (oundry.project_name → oundry.project, ision_model_deployment → ision_deployment)
  3. Graph-to-Search columns defined in SPEC-002 but binding contract missing in SPEC-003 (canonical binding established)
  4. Domain prompt security requirement not specified (added: user prompt role only, never system prompt)

- **2 MINOR findings resolved:**
  1. Ontology service name compile-search-index vs command name inconsistency
  2. Vision model documentation vague on default

---

## Canonical Decisions Locked

| Category | Decision |
|----------|----------|
| **CLI Framework** | Click 8.x |
| **Pipeline Stages** | 11-stage sequence: domain-intake → inspect-source → enrich → compile-data → compile-ontology → compile-search → package → deploy-lakehouse → deploy-ontology → deploy-search → validate |
| **LLM SDK** | Microsoft Foundry SDK (zure-ai-projects) |
| **Chat Model** | GPT-5.5-mini (target) / gpt-4.1 (interim dev) |
| **Embedding Model** | text-embedding-3-large @ 1536 dimensions |
| **Vision Model** | Chat deployment (multimodal; gpt-4.1 interim / 5.5-mini target); siq-vision/gpt-4o alternative |
| **Config Format** | fabric-kg.yaml (non-secret) + .env (secrets) |
| **Data IDs** | Deterministic SHA-256 hashes |
| **Parquet Arrays** | Native pa.list_(pa.string()), not JSON |
| **Structured Tables** | Lakehouse-only, never indexed to AI Search |
| **Graph-to-Search Filter** | search.in(entity_ids, '...', ',') (not ny(... eq ...)) |
| **Document Intelligence** | Required dependency for visual_regions |
| **Validation Rules** | 22 fail / 8 warn |
| **Test Coverage** | 90% line coverage gate (src/ only) |
| **Test APIs** | All mocked (no live calls) |

---

## Action Items (S1 Pre-work)

| Priority | Item | Owner |
|----------|------|-------|
| 🔴 CRITICAL | Deploy GPT-5.5-mini to example-aiservices Foundry | Hyunsuk |
| 🔴 CRITICAL | Create Lakehouse item in dev workspace | Keyser |
| 🟡 HIGH | Populate ontology/environments/dev.json lakehouse_item_id | Hyunsuk |
| 🟡 HIGH | Verify Foundry endpoint URL | McManus / Hyunsuk |
| 🟢 LOW | Verify example-docintell & example-vision endpoints | McManus / Hyunsuk |

---

## Next Steps

1. **S1 Ready:** All specs locked, planning complete. PLAN-001 ready to execute once GPT-5.5-mini deployed and Lakehouse item provisioned.
2. **Team Review:** All agents should review consolidated decisions.md for accuracy.
3. **Acceptance Criteria:** 13 PRD §23 criteria mapped to M0–M4 (MVP). All locked as merge gates.
4. **Sprint 1 Kickoff:** Begin M0 (pyproject setup, CLI skeleton) once infrastructure prerequisites met.
