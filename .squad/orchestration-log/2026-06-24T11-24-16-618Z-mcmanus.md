# Orchestration Log — McManus (KG/Ontology Dev)

**UTC Timestamp:** 2026-06-24T11:24:16.618Z  
**Role:** KG/Ontology Developer

## Session Summary

McManus authored the ontology and deployment specification this session:

1. **docs/specs/SPEC-003-ontology-and-deployment.md** — Ontology definition and Fabric deployment strategy. Four core decisions locked:
   - `blob_url` is a node property (not a relationship edge)
   - Inverse relationships: explicit policy (none | materialize | alias) — required field
   - Environment config injected at compile time (not deploy time)
   - ID ranges prefix-based: loose module grouping (not strict per-module sub-ranges)

2. Decision inbox entry **mcmanus-spec-ontology.md** merged into `.squad/decisions.md`.

## Decisions Made

- blob_url property strategy (finalized)
- inversePolicy explicit enumeration (finalized) — fixes AI Search traversal loss
- Compile-time env injection (finalized)
- ID range strategy established (finalized)

## Coordination Points

- inversePolicy requirement adds mandatory field to model.yaml (all relationship types must declare)
- Compiler enforcement prevents silent regression (addresses AI Search prototype issue)
- Environment-specific artifacts require separate CI/CD builds per env (dev, test, prod)

## Open Questions

1. Should compiler support `--dry-run` flag?
2. Should `build/ontology/` be committed to source or treated as generated-only?
3. Should Fabric sensitivity label GUID be in `env.json` or resolved at deploy time?

## Status

DONE — Specification complete and merged into decisions record.
