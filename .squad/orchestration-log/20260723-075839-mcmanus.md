# McManus Orchestration Log

**Date:** 2026-07-23T07:58:39Z  
**Session:** febebfe8-714a-4b2f-909d-6f68ae80715e  
**Role:** KG/Ontology Dev (sync, revision owner)  
**Status:** Complete — locked-out revision, all blockers fixed, 2409 tests passing

## Tasks Executed

### 1. Review Protocol Assessment
- Assigned as revision owner (different author) following Hockney's formal rejection of Verbal's implementation
- Locked-out status: Verbal cannot revise this artifact; only McManus edits production code
- Focus: Address 2 RED tests by fixing production gaps (not re-implementing existing passing 180 tests)

### 2. Blocker 1 — Unhandled `DataAgentRequiredExampleEmpty` at CLI Boundaries

**File:** `cli/deploy_cmd.py`, `cli/build_deploy_cmd.py`

**Problem:** Exception escaped uncaught in try/except that only caught `(AgentPublicationError, OSError, ValueError)`. `DataAgentRequiredExampleEmpty` is a `ValidationError` subclass — neither OSError nor ValueError.

**Fix:**
- Added early deferred import of `DataAgentRequiredExampleEmpty` before grounding try block
- Added dedicated except clause: `except DataAgentRequiredExampleEmpty as exc` → raises `ClickException(str(exc))` (deploy) or `BuildDeployError(str(exc))` (build-deploy)
- Applied to dry-run and live paths (same grounding try block runs in both)

**Impact:** Operators now see structured error message instead of raw traceback on required-empty condition

### 3. Blocker 2 — `_capability_availability` Not Wired Into Text Builders

**File:** `cli/deploy_cmd.py`, `cli/build_deploy_cmd.py`

**Problem:** Both CLI paths computed availability dict from materialization plan but called text builders without `availability=` kwarg. Functions accept this parameter (implemented by Verbal) but received `None`, so deployed text used generic fallback — still capable of over-claiming unavailable relationships.

**Fix:**
- Added `availability=_capability_availability or None` to both `build_graph_source_instructions` and `build_graph_source_description` call sites
- Applied in both CLI files (parity requirement D1)

**Impact:** Deployed text now correctly parameterized by observed capability; no over-claims of unavailable relationships

### 4. Blocker 3 — Property Selection Hashes Count-Only

**Files:** `knowledge/data_agent.py`, `knowledge/agent_validation.py`

**Problem:** Hashes computed as `_canonical_hash({"property_child_count": N})`. Two selections with same number of properties but different IDs produce same hash — silent audit failure.

**Fix:**
- Added `selected_property_ids: list[str]` property to `DataAgentStageSnapshot` in `data_agent.py`
- Returns sorted canonical property child IDs across all selected elements using existing private module functions
- Changed hash computation in `build_agent_publication_receipt` to:
  ```python
  compiled_prop_sel_hash = _canonical_hash({"property_ids": expected.selected_property_ids})
  published_prop_sel_hash = _canonical_hash({"property_ids": published.selected_property_ids})
  ```

**Impact:** Different property selections now produce different hashes; audit integrity maintained

### 5. Blocker 4 — Compiled Property Count Not Validated Before Draft/Published

**File:** `knowledge/agent_validation.py`

**Problem:** `build_agent_publication_receipt` only compared draft and published counts against `grounding.expected_property_child_count`. Compiled omission would not be caught until live publish step.

**Fix:**
- Added compiled property count check immediately after counts set, before draft/published checks
- Raises `AgentPublicationError` if `compiled_count != required_count` with clear remediation message

**Impact:** Compiled property omissions now caught pre-flight (dry-run); no silent false-passes

### 6. Dependency File Cleanup

**Files:** `pyproject.toml`, `uv.lock`

**Problem:** Duplicate `[dependency-groups] dev` section added by Verbal, mirroring `[project.optional-dependencies]`. Not required; test runner functioned without it.

**Fix:** Reverted both files to baseline. Verbal's dependency changes removed.

**Impact:** Cleaner manifest; no technical change to build/test

### 7. Optional-Absent Filtering Verification

**File:** `knowledge/data_agent.py::graph_few_shots_from_competency_contract`

**Status:** Already correctly implemented by Verbal's gating function. The receipts returned by `gate_competency_examples` are now used to build published_ids set; cases not in that set are skipped.

**Impact:** The two RED tests now GREEN (Hockney's verification pending, expected to confirm)

## Test Results

| Scope | Count | Status |
|-------|-------|--------|
| Five-file targeted suite | 202 | ✅ All PASS (184 existing + 18 new) |
| Full suite | 2409 | ✅ All PASS, 4 deselected, 5 warnings (pre-existing) |

## New Regression Tests Added (18 in `test_agent_contract_validation.py`)

- `TestPropertySelectionHashContentBased` (5): sorted IDs, empty list, different-IDs-different-hashes, same-IDs-same-hash, valid SHA256 format
- `TestCompiledPropertyOmissionBlocks` (2): compiled omission raises, equal counts pass
- `TestRequiredExampleEmptyBoundaryClassification` (4): error type, actionable message, structured attributes accessible
- `TestGraphSourceAvailabilityWiring` (6): both builders accept availability, unavailable rels in instructions, available rels in description, no-availability fallback, unavailable-only warning

## Outcomes

- **All four blockers resolved**
- **All RED tests expected to turn GREEN** (confirmed by updated inline gating + receiver filtering)
- **Full suite green**: 2409 passing, zero failures
- **Parity maintained**: Both CLI paths identical in availability wiring and error handling
- **Backward compatibility**: No breaking changes; existing behavior preserved when availability=None

## Status

Ready for merge. All blockers closed. Hockney (independent reviewer) to formally verify test results and approve.

