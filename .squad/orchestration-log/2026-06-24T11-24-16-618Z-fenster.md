# Orchestration Log — Fenster (Data Engineer)

**UTC Timestamp:** 2026-06-24T11:24:16.618Z  
**Role:** Data Engineer

## Session Summary

Fenster authored the canonical data model specification this session:

1. **docs/specs/SPEC-002-canonical-data-model.md** — Canonical Parquet schema for all 8 tables. Five core decisions locked:
   - All data row IDs: deterministic SHA-256 hashes (not UUIDs)
   - Entity canonical key normalization: pinned algorithm
   - list<string> columns: native Parquet arrays (not JSON strings)
   - Placeholder layout: `{table}/_placeholder.parquet`
   - Validation severity: 22 fail / 8 warn rules

2. Decision inbox entry **fenster-spec-data-model.md** merged into `.squad/decisions.md`.

## Decisions Made

- Data row ID strategy: `{prefix}:{sha256(canonical_string)[:32]}` (finalized)
- Canonical key normalization locked in spec (breaking changes require version bump)
- Native Parquet arrays for list columns (finalized)
- Validation severity matrix established (finalized)

## Coordination Points

- ID hashing algorithm is stable; Verbal's `id_hint` handoff contract is documented
- Fenster's decision on ID strategy removes ambiguity from Verbal's LLM enrichment spec

## Open Questions

1. Should `properties_json` be replaced with `pa.map_` in future schema version?
2. Should `related_entity_ids` be a junction table rather than an array column?
3. Should `image_hash` uniqueness be enforced globally or per-source?
4. Should schema-profile.json format be versioned separately?

## Status

DONE — Specification complete and merged into decisions record.
