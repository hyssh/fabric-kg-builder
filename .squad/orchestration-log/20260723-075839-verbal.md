# Verbal Orchestration Log

**Date:** 2026-07-23T07:58:39Z  
**Session:** febebfe8-714a-4b2f-909d-6f68ae80715e  
**Role:** AI Integration Dev (background mode)  
**Status:** Complete (initially), then locked out; fixes completed by McManus revision

## Tasks Executed

### 1. Schemas & Vocabulary (D3) — Foundation
- Added to `semantic/schemas.py`:
  - `CompetencyExampleStatus` enum (pass, published, blocked, skipped, omitted)
  - `CompetencyExampleReceipt` with `competency_id`, `required`, `required_relationship_ids`, `observed_rows`, `min_required_rows`, `status`, `remediation`, `published`
- Extended existing `QueryReadiness` with `observed_relationship_rows: dict[str, int]`
- Extended `AgentPublicationReceipt` with property/text-count fields

### 2. Error Types (D4) — Validation Spine
- Added to `knowledge/validation.py`:
  - `DataAgentPropertyOmitted` — required property missing from persisted/draft/published
  - `DataAgentRequiredExampleEmpty` — required competency example with observed rows below minimum
  - `DataAgentUnavailableRelationshipClaimed` — generated description references unavailable relationship
  - `classify_relationship_availability` pure helper (4-state classification)

### 3. Property Preservation (#14, D5) — Most Self-Contained
- Modified `knowledge/agent_validation.py::build_public_graph_source_projection`:
  - Stopped stripping `children` from graph elements
  - Projected selected, `agent_visible` property children into Fabric-accepted shape
- Modified `build_agent_publication_receipt`:
  - Implemented three-way property count comparison (required vs draft vs published)
  - Added property selection hash fields (computed from selected property IDs, not count)
  - Added compiled property omission check

### 4. Example Gating (#13, D6) — Cross-Cutting
- Implemented `gate_competency_examples(contract, availability, *, min_required_rows=1)` in `knowledge/validation.py`
- Reused `runtime/acceptance.py` case parsing for requirement detection
- Extended `semantic/persisted_projection.py::validate_graph_query_readiness` to capture per-relationship observed row counts
- Modified `data_agent.py::graph_few_shots_from_competency_contract`:
  - Accepts optional `availability` parameter
  - Uses gating receipts to filter optional-absent cases

### 5. Capability-Aware Text (#12, D7) — Text Builders
- Modified `semantic/instructions.py`:
  - Parameterized `build_graph_source_description`, `build_graph_source_instructions`, `build_ontology_source_*` with optional `availability`
  - Descriptions enumerate only observed-available relationships
  - Unavailable-but-existing relationships explicitly named ("X relationships exist but have no verified published data")
  - Global instructions remain routing/evidence/refusal/failure only

### 6. CLI Wiring — Integration Point
- Modified `cli/deploy_cmd.py`:
  - Computed `_capability_availability` dict from materialization plan
  - Passed to `graph_few_shots_from_competency_contract` and text builders
  - Passed char-count dicts to `deploy_and_validate_data_agent`
- Modified `cli/build_deploy_cmd.py` in parallel (D1 parity requirement)

### 7. Dry-Run Reporting (D8)
- Extended both CLI paths to append property selection, grounding text, and example gating status to existing dry-run block

## Intermediate Test Results

- Targeted suite (5 files): 184 existing + partial new = 182 tests passing initially
- **2 RED tests identified by Hockney:**
  - `test_optional_case_with_zero_rows_is_silently_omitted`
  - `test_unrelated_required_case_unaffected_by_unavailable_optional`
- Root cause: `graph_few_shots_from_competency_contract` not using receipts to filter optional-absent cases (noted in Hockney's decision note)

## Status Change

**Initial implementation complete (182/184 passing).** Hockney's review identified that optional filtering was incomplete and assigned revision to a different agent per protocol.

Verbal locked out; handoff to McManus for production blockers.

## Handoff

All production code changes owned. Hockney authors independent tests. McManus (revision owner) to address blocker gaps if needed.

